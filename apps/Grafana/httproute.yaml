
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: monitoring-httproute
  namespace: monitoring
spec:
  parentRefs:
    - name: public-gateway
      namespace: envoy-gateway-system
#     sectionName: http
  hostnames:
    - "monitoring.local" #il tuo host
  rules:
    # Regola principale per SSR su /
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: grafana
          namespace: monitoring
          port: 3000
