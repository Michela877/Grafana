apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: grafana
  namespace: flux-system
spec:
  interval: 1m
  path: "./apps/Grafana"
  prune: true
  sourceRef:
    kind: GitRepository
    name: monitoring-repo
